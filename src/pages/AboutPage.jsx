import React from 'react';
import About from '../components/About';

const AboutPage = () => {
    return (
        <main style={{ paddingTop: '6rem', minHeight: '100vh', backgroundColor: '#000' }}>
            <About />
        </main>
    );
};

export default AboutPage;
