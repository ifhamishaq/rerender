import React from 'react';
import About from '../components/About';

const AboutPage = () => {
    return (
        <main style={{ paddingTop: '6rem', minHeight: '100vh', backgroundColor: 'var(--color-bg)' }}>
            <About />
        </main>
    );
};

export default AboutPage;
